import { FilterQuestTypesPipe } from './filter-quest-types.pipe';

describe('FilterQuestTypesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterQuestTypesPipe();
    expect(pipe).toBeTruthy();
  });
});
